{
  "name": "mill-smart-log-e2e-web",
  "version": "0.1.0",
  "private": true,
  "description": "Browser (Playwright) tests for the Laravel/Livewire web app. Converted 2026-09-16 from backend/tests/Browser, which held Playwright JavaScript inside .php files that PHPUnit could never execute.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
