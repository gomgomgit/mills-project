## v1 — 2026-08-19

- Route `/mill-settings` and 4 dedicated endpoints under `/api/mill-settings/:business_unit_id/...` ← not stated by user; derived from naming conventions in shared-decisions. IMPORTANT: initial draft used "mills-setting" (wrong pluralization) — corrected mid-flow after discovering a sibling fork (screen-005--home) had already registered `GET /api/mill-settings/current` in api-index with the correct "mill-settings" plural and bare `logo`/`home_page_image` response field names (no `_url` suffix, unlike Machinery's `picture_url`). Rewrote this screen's tech spec to match that established naming exactly, and designed the 4 new endpoints as parameterized companions to (not duplicates of) `/current`.
- Chose to make the station-image endpoints (`GET .../stations`, `POST .../stations/:id/image`) SEPARATE from screen-030's existing `/api/stations` (Admin-only) rather than reusing it ← technical decision to avoid opening full Kelola Station access to Mill Management just to let them upload station images for their own mill.
- Lazy auto-create of the default mill-setting row (on first GET/PATCH) rather than backfilling all existing Business Units at migration time ← technical choice to avoid a data migration step; keeps existing Business Units automatically compatible.
- `unit_test_cases` (16) and `test_scenarios` (7) derived manually by this agent rather than via `test-spec-writer-agent` — forks cannot spawn subagents. Followed the same derivation pattern the agent would use (from api_contracts + bdd_scenarios). Flagged per the Derived Assumptions Log convention, same constraint noted by sibling forks earlier in this session.
- `GET /api/business-units/options` (already registered under screen-030) reused for the Admin mill-picker rather than re-registering a duplicate endpoint under this screen.

## v2 — 2026-08-19

- REVISI from user (relayed by coordinator): `station.image` (multipart upload endpoint) replaced with `station.icon` per entity-catalog v7. `POST .../stations/:station_id/image` (file upload) → `PATCH .../stations/:station_id` (JSON body `{icon: string|null}`), no file storage involved.
- Chose a fixed allow-list for valid icon names (gauge, layers, package, truck, scale, warehouse, factory, container, box, boxes) rather than accepting any arbitrary Lucide icon name ← technical decision to keep validation simple and the FE picker's option list bounded; not stated by user, extensible later if needed.
- `icon: null` explicitly resets a station to its type-default icon (matching uiux-spec component_patterns 'station-tile' fallback behavior already documented) ← direct implication of the coordinator's correction, not separately re-stated by the user.
- No existing icon-picker UI convention found in the codebase — FE implementation note says to use a simple dropdown/select of supported icon names; deferred as an implementation detail, not a business-spec decision.
- Re-derived 3 of 16 unit_test_cases (the ones specific to station image upload) to match icon-set behavior instead: file-store test → icon-set test, format-validation test → allow-list-validation test, added a new reset-to-default (icon=null) test. The remaining 13 unit tests (mill-setting CRUD, ownership/403, logo/home_page_image upload) are unaffected and unchanged.
